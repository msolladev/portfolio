version: "3.9"

services:
  portfolio:
    image: portfolio:latest
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - web

networks:
  web:
    external: true